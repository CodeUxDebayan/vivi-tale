Make a authentication to hash the password and use mailgun to send an otp that can be checked in for a minute can be resend, count and show that how many messages are left for the day via Mailgun Metrics API  to query your processed_count for the current day.Logic: Subtract this count from your daily limit (e.g., 100 for the free tier).Endpoint: GET /v1/analytics/usage/metrics.